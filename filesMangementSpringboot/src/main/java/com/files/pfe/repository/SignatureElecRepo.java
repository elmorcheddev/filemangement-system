package com.files.pfe.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.files.pfe.model.SignatureElec;
 
public interface SignatureElecRepo extends JpaRepository<SignatureElec, Long> {
	
}
